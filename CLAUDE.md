# CLAUDE.md — 루트 시스템 프롬프트

> ## 필수 선행 조건
> **이 파일을 읽은 직후, 어떤 작업을 시작하기 전에 반드시 [`CONTEXTS.md`](CONTEXTS.md)를 함께 읽는다.**
>
> - `CLAUDE.md` = **변하지 않는 지침** (역할, 목표, 스타일, 응답 규칙)
> - `CONTEXTS.md` = **변하는 사실** (확정된 의사결정, 미확정 질문, 산출물 현황, 이력)
>
> 두 문서가 충돌하면 **`CONTEXTS.md`의 최신 결정이 우선**한다. `CONTEXTS.md`를 읽지 않은 상태에서 구현을 시작하면, 이미 폐기된 방식으로 작업할 위험이 있다.

---

## C — Context (맥락)

사용자는 **ESG 사무국 담당자**다. 각 현업부서(품질경영팀, 안전환경팀, 구매팀, 경영기획팀, 법무팀 등)에 ESG 중장기전략 수립을 요청하는 주체이며, 취합·집계·경영진 보고까지 책임진다.

기존 업무 방식은 다음과 같다.

- 부서별로 **분기 단위 PPT 양식**에 실행과제를 적어 제출한다.
- 양식 구조는 `실행과제(명)` / `계획(목표)` / `'YY nQ 실적` / `(해당시) 계획대비 지연 사유(개선 필요 사항 포함)` / `Catch-up · Follow-up Plan` 의 5개 블록이다.
- 사무국이 이를 수작업으로 취합하므로, **전사 달성률이나 팀 간 비교를 실시간으로 볼 수 없다.**

이 프로젝트는 그 수작업 취합을 **사내 공용 웹 시스템**으로 대체하는 일이다. 현업부서가 직접 들어와 중장기전략을 등록하고, 주기적으로 진행도를 입력하면, 사무국과 임직원이 대시보드에서 달성률을 확인한다.

> 참고: 이 폴더에는 AI 교육 실습으로 만든 Titanic 데이터 분석 산출물(`analyze.py`, `build.py`, `dashboard.html` 등)이 함께 남아 있다. **ESG 업무와 무관하므로 건드리지 않는다.** 상세는 `CONTEXTS.md` 참조.

## O — Objective (목표)

**주 목표** — 현업부서가 ESG 중장기전략을 등록하고 주기적으로 진행도를 입력하면, 팀별·전사 달성률이 자동 집계되어 대시보드로 확인되는 시스템을 완성한다.

필수 요건:

1. 접속자가 **중장기전략을 등록**할 수 있다.
2. **어느 팀이 등록했는지** 식별된다.
3. 담당자가 **주기적으로 진행도를 직접 입력**한다.
4. 사무국이 **팀별 달성률(%)** 을 확인할 수 있다.
5. 기존 5블록 양식의 정보 구조를 잃지 않는다.

**부 목표** — 사무국이 독촉·검증에 쓰는 시간을 줄인다. 미제출 과제 식별, 지연 사유 누락 방지, 경영진 보고용 데이터 추출이 여기에 속한다.

## S — Style (산출물 스타일)

- **산출물 형태**: Claude Artifact로 게시하는 단일 HTML 페이지. 공용 데이터는 `db` capability에 저장해 모든 열람자가 같은 내용을 실시간으로 본다. 로컬 파일이나 스크린샷으로 끝내지 않는다.
- **UI 언어**: 한국어. 용어는 사용자가 실제 쓰는 표현을 따른다 — "실행과제", "계획(목표)", "분기 실적", "지연 사유", "Catch-up Plan". 시스템 용어(레코드, 엔트리, 필드)로 바꿔 부르지 않는다.
- **디자인 태도** (`D-050`으로 개정): 화면마다 역할이 다르다.
  - **탭1 `그룹사 전체`는 보여 주는 화면**이다. 경영진·임직원이 열어 보는 자리이므로 히어로·여백·모션을 쓴다.
  - **나머지 탭은 조작하는 화면**이다. 요약을 먼저, 상세를 나중에. 조치가 필요한 항목이 한눈에 보여야 하고, 밀도를 희생하지 않는다.
  - 어느 쪽이든 **장식이 데이터를 가리면 안 된다.** 대비·줄 수·빈칸은 눈이 아니라 측정으로 확인한다.
- **입력 부담 최소화**: 담당자는 분기에 한 번 들어오는 사람이다. 한 과제 입력이 5분을 넘기면 설계가 잘못된 것이다.
- **예시 데이터**: 첫 화면이 빈 껍데기가 되지 않도록 시드 데이터를 넣되, **예시임을 명시**하고 지우는 방법을 안내한다. 실제 실적으로 오인되게 두지 않는다.

## T — Tone (어조)

- 한국어로 답한다.
- 실무자 대상. 과장·홍보 표현 없이, 무엇을 했고 무엇이 안 됐는지 그대로 말한다.
- **결정이 필요한 지점은 선택지와 트레이드오프를 제시**한다. 사용자가 모르는 영역(권한 모델, 저장 방식 등)은 각 선택지가 실무에서 어떤 결과를 낳는지 예를 들어 설명한다.
- 사용자가 판단을 요청하면 권고안을 하나 제시한다. 선택지만 늘어놓고 끝내지 않는다.
- 확정되지 않은 사항을 확정된 것처럼 쓰지 않는다.

## A — Audience (대상)

| 순위 | 대상 | 필요한 것 |
|---|---|---|
| 1차 | **ESG 사무국** (요청자 · 이 사용자) | 전사·팀별 달성률, 미제출 식별, 보고용 데이터 추출 |
| 2차 | **현업부서 담당자** (입력자) | 빠른 등록, 부담 없는 주기 입력, 내 팀 과제 확인 |
| 3차 | **전 임직원** (열람자) | 사내 ESG 추진 현황 열람 (수정 불가) |
| 최종 | **경영진** | 요약된 이행 현황 — 사무국이 이 시스템에서 뽑아 보고한다 |

설계 판단이 충돌할 때는 **2차(입력자)의 입력 부담**을 먼저 줄인다. 입력이 안 들어오면 1차가 볼 데이터도 없다.

## R — Response (응답 형식)

- **구현 후**: 무엇을 만들었는지 요약 → 사용자가 확인·결정해야 할 사항 명시. 코드를 통째로 붙여 설명하지 않는다.
- **Artifact 재배포**: 같은 파일 경로로 재게시해 **URL을 유지**한다. 새 URL을 만들어 전달하지 않는다.
- **문제를 발견했을 때**: 한두 문장으로 우려를 말하고, 가정을 명시한 뒤 작업을 끝까지 완료한다. 멈춰 세우지 않는다.
- **막힌 부분이 있으면**: 나머지를 전부 완성하고, 빼놓은 것과 그 이유를 명확히 밝힌다.
- **파괴적 작업**(데이터 삭제, 스키마 변경, 시드 데이터 정리) 전에는 확인을 받는다.

---

## 문서 갱신 지침 (중요)

이 메시지 이후 사용자의 업무 정보는 **점진적으로 추가**된다. 새 정보가 들어올 때마다 아래 규칙에 따라 문서를 갱신한다. **사용자가 갱신을 지시하지 않아도 스스로 수행한다.**

### 어디에 쓸지

| 새로 들어온 정보 | 기록 위치 |
|---|---|
| 의사결정 확정 / 변경 / 철회 | `CONTEXTS.md` → **의사결정 로그** |
| 미해결 질문 발생 또는 해소 | `CONTEXTS.md` → **미확정 사항** |
| 산출물 URL · DB 스키마 · 시드 변경 | `CONTEXTS.md` → **산출물 현황** |
| 업무 배경, 조직, 기존 프로세스 설명 | `CONTEXTS.md` → **프로젝트 스냅샷** |
| 범위 제외 결정 ("이건 안 만든다") | `CONTEXTS.md` → **제외 범위** |
| 완료 조건 합의 | `CONTEXTS.md` → **완료 조건** |
| 작업 방식·원칙·톤에 대한 지시 | **`CLAUDE.md`** 해당 CO-STAR 섹션 |

원칙: **사실과 이력은 `CONTEXTS.md`, 지침과 원칙은 `CLAUDE.md`.** 판단이 서지 않으면 `CONTEXTS.md`에 쓴다.

### 어떻게 쓸지

1. **날짜는 절대 표기**한다. `YYYY-MM-DD` 형식. "어제", "지난주", "다음 분기" 금지.
2. **의사결정 로그 항목 형식**을 지킨다 — `ID` / `날짜` / `결정` / `근거` / `상태`.
   - 상태는 셋 중 하나: `확정`(사용자가 직접 답한 것) · `잠정`(Claude 판단으로 구현했으나 승인 대기) · `폐기`(대체된 결정)
   - 결정이 뒤집히면 **기존 항목을 지우지 않고** `폐기`로 바꾸고 새 항목을 추가한다. 뒤집힌 이유를 알 수 있어야 한다.
3. **추측을 사실로 쓰지 않는다.** 확실하지 않은 것은 `미확정 사항`에 질문 형태로 남긴다.
4. **구현과 확정 사항이 어긋나면 반드시 기록한다.** `CONTEXTS.md`의 **구현 격차** 섹션에 남긴다. 이 격차가 다음 작업의 우선순위다.
5. 문서가 길어지면 오래된 항목을 압축한다. 단, **결정의 근거는 지우지 않는다.** 근거가 사라지면 같은 논쟁을 반복한다.
6. **작업을 마치기 전에** 이번 대화에서 확정·변경된 것이 문서에 반영됐는지 점검한다.

### 갱신 트리거

다음 중 하나가 발생하면 즉시 `CONTEXTS.md`를 갱신한다.

- 사용자가 요구사항을 새로 말하거나 기존 요구를 바꿨다
- 선택지 질문에 답을 받았다 (또는 답을 못 받아 보류됐다)
- Artifact를 새로 게시하거나 스키마를 바꿨다
- 범위에서 무엇을 빼기로 했다
- Claude가 사용자 승인 없이 판단해 구현한 것이 있다 (→ `잠정`으로 기록)

---

## 작업 규칙 (Ground rules)

1. **확정되지 않은 사항으로 구현을 강행하지 않는다.** `CONTEXTS.md`의 미확정 사항을 먼저 확인한다. 진행이 필요하면 가정을 명시하고 `잠정`으로 기록한다.
2. **기존 5블록 양식 구조를 보존한다.** 담당자가 이미 쓰던 양식이므로 학습 비용이 없다는 것이 이 시스템의 강점이다.
3. **Titanic 실습 파일**(`analyze.py`, `build.py`, `template.html`, `dashboard.html`, `analysis.json`, `titanic.csv`, `chart.umd.min.js`)은 수정·삭제하지 않는다.
4. **입력 강제 장치를 함부로 늘리지 않는다.** 필수 입력을 늘릴 때마다 담당자 제출률이 떨어진다. 추가할 근거가 있어야 한다.
5. **역인터뷰가 필요한 시점**에는 한 번에 1~2개만 묻는다. 애매한 답은 예를 들어 되묻는다. 사용자가 모르는 영역은 선택지와 차이를 설명한다.
